# Production Deployment Checklist

Контрольный список для развертывания Telegram Lead Bot на продакшн-сервере.

## 📋 Подготовка перед развертыванием

### 1. Документация
- [ ] Прочитан [QUICKSTART.md](QUICKSTART.md)
- [ ] Прочитан [DEPLOYMENT.md](DEPLOYMENT.md)
- [ ] Изучены скрипты в [scripts/README.md](scripts/README.md)

### 2. Учетные данные
- [ ] Получен BOT_TOKEN от [@BotFather](https://t.me/BotFather)
- [ ] Получен ADMIN_CHAT_ID от [@userinfobot](https://t.me/userinfobot)
- [ ] Протестирован бот локально
- [ ] Бот отвечает на команды `/start`, `/help`

### 3. Сервер
- [ ] Арендован VPS сервер
- [ ] Записан IP адрес сервера: `_________________`
- [ ] Записаны credentials для доступа
- [ ] Проверено подключение к серверу по SSH

---

## 🖥️ Настройка сервера

### 1. Базовая настройка

```bash
# Обновление системы
[ ] sudo apt update && sudo apt upgrade -y

# Установка необходимых пакетов
[ ] sudo apt install -y python3.11 python3.11-venv git sqlite3 htop curl wget

# Настройка файрвола
[ ] sudo ufw allow 22/tcp
[ ] sudo ufw enable
[ ] sudo ufw status
```

### 2. Создание пользователя

```bash
# Создать отдельного пользователя для бота
[ ] sudo adduser botuser

# Переключиться на пользователя
[ ] su - botuser
```

### 3. SSH безопасность

```bash
# На локальной машине: генерация SSH ключа
[ ] ssh-keygen -t ed25519 -C "your_email@example.com"

# Копирование ключа на сервер
[ ] ssh-copy-id botuser@server_ip

# На сервере: отключение входа по паролю
[ ] sudo nano /etc/ssh/sshd_config
    # Установить: PasswordAuthentication no
[ ] sudo systemctl restart sshd

# Проверка входа по ключу (из новой сессии)
[ ] ssh botuser@server_ip
```

---

## 📦 Установка бота

### Выберите метод развертывания:

- [ ] **Вариант A: systemd** (рекомендуется для простоты)
- [ ] **Вариант B: Docker** (рекомендуется для изоляции)

---

## Вариант A: systemd Service

### 1. Клонирование и настройка

```bash
[ ] cd /home/botuser
[ ] git clone https://github.com/yourusername/telegram-lead-bot.git
[ ] cd telegram-lead-bot

# Создание виртуального окружения
[ ] python3.11 -m venv venv
[ ] source venv/bin/activate
[ ] pip install --upgrade pip
[ ] pip install -r requirements.txt
```

### 2. Конфигурация

```bash
# Создание .env файла
[ ] nano .env
    # Вставить:
    # BOT_TOKEN=your_token
    # ADMIN_CHAT_ID=your_id
    # TIMEZONE=Europe/Podgorica

# Установка правильных прав
[ ] chmod 600 .env
[ ] chown botuser:botuser .env
```

### 3. Тестовый запуск

```bash
# Запустить бота вручную для проверки
[ ] source venv/bin/activate
[ ] python -m app.bot
    # Проверить, что бот отвечает в Telegram
    # Ctrl+C для остановки
```

### 4. Создание systemd service

```bash
# Копирование service файла
[ ] sudo cp scripts/telegram-lead-bot.service /etc/systemd/system/

# Проверка и редактирование путей (если нужно)
[ ] sudo nano /etc/systemd/system/telegram-lead-bot.service
    # Проверить: User, WorkingDirectory, ExecStart

# Перезагрузка systemd
[ ] sudo systemctl daemon-reload

# Запуск сервиса
[ ] sudo systemctl start telegram-lead-bot

# Проверка статуса
[ ] sudo systemctl status telegram-lead-bot
    # Должен быть: active (running)

# Включение автозапуска
[ ] sudo systemctl enable telegram-lead-bot
```

### 5. Проверка работы

```bash
# Просмотр логов
[ ] sudo journalctl -u telegram-lead-bot -f

# Отправить сообщение боту в Telegram
[ ] Отправить /start боту
[ ] Создать тестовую заявку
[ ] Проверить получение уведомления админом

# Проверка базы данных
[ ] sqlite3 leads.db "SELECT COUNT(*) FROM leads;"
```

---

## Вариант B: Docker

### 1. Установка Docker

```bash
[ ] curl -fsSL https://get.docker.com -o get-docker.sh
[ ] sudo sh get-docker.sh
[ ] sudo usermod -aG docker botuser
[ ] exit  # Перелогиниться
[ ] su - botuser

# Проверка
[ ] docker --version
[ ] docker compose version
```

### 2. Клонирование и настройка

```bash
[ ] cd /home/botuser
[ ] git clone https://github.com/yourusername/telegram-lead-bot.git
[ ] cd telegram-lead-bot

# Создание .env файла
[ ] nano .env
    # Вставить: BOT_TOKEN, ADMIN_CHAT_ID, TIMEZONE

# Создание директории для данных
[ ] mkdir -p data
```

### 3. Запуск контейнера

```bash
# Сборка и запуск
[ ] docker compose up -d

# Проверка статуса
[ ] docker compose ps
    # Должен быть: Up

# Просмотр логов
[ ] docker compose logs -f
```

### 4. Проверка работы

```bash
# Отправить сообщение боту в Telegram
[ ] Отправить /start боту
[ ] Создать тестовую заявку
[ ] Проверить получение уведомления админом

# Проверка базы данных
[ ] docker compose exec telegram-bot sqlite3 /data/leads.db "SELECT COUNT(*) FROM leads;"
```

---

## 🔄 Настройка Backup'ов

### 1. Подготовка скриптов

```bash
# Сделать скрипты исполняемыми
[ ] chmod +x scripts/*.sh

# Создать директорию для backup'ов
[ ] mkdir -p /home/botuser/backups
```

### 2. Тестирование backup

```bash
# Запустить backup вручную
[ ] ./scripts/backup-bot.sh

# Проверить созданный backup
[ ] ls -lh /home/botuser/backups/
[ ] Должен появиться файл leads_backup_*.db.gz
```

### 3. Настройка автоматических backup'ов

```bash
# Открыть crontab
[ ] crontab -e

# Добавить строки:
[ ] 0 3 * * * /home/botuser/telegram-lead-bot/scripts/backup-bot.sh >> /home/botuser/backups/backup.log 2>&1

# Проверить, что задача добавлена
[ ] crontab -l
```

### 4. Тестирование восстановления

```bash
# Найти последний backup
[ ] ls -lt /home/botuser/backups/*.db.gz | head -1

# Восстановить из backup (для теста)
[ ] ./scripts/restore-backup.sh /home/botuser/backups/leads_backup_*.db.gz
```

---

## 📊 Настройка мониторинга

### 1. Health Check

```bash
# Запустить health check вручную
[ ] ./scripts/check-bot-health.sh

# Проверить вывод:
    [ ] ✓ Service is running
    [ ] ✓ Database file exists
    [ ] ✓ Database integrity OK
```

### 2. Автоматический мониторинг (опционально)

```bash
# Добавить в crontab
[ ] crontab -e
[ ] */5 * * * * /home/botuser/telegram-lead-bot/scripts/check-bot-health.sh >> /home/botuser/health-check.log 2>&1
```

### 3. Telegram алерты (опционально)

```bash
# Настроить telegram-alert.sh
[ ] nano scripts/telegram-alert.sh
    # Изменить: BOT_TOKEN, CHAT_ID

# Протестировать
[ ] ./scripts/telegram-alert.sh "Test alert from server"

# Проверить получение сообщения в Telegram
[ ] Получено тестовое сообщение
```

---

## 🔐 Безопасность

### 1. Права доступа

```bash
# Проверить и установить правильные права
[ ] chmod 600 /home/botuser/telegram-lead-bot/.env
[ ] chmod 600 /home/botuser/telegram-lead-bot/leads.db
[ ] ls -la /home/botuser/telegram-lead-bot/
```

### 2. Автоматические обновления безопасности

```bash
# Установить unattended-upgrades
[ ] sudo apt install unattended-upgrades
[ ] sudo dpkg-reconfigure --priority=low unattended-upgrades
```

### 3. Fail2ban (опционально)

```bash
# Установка
[ ] sudo apt install fail2ban

# Настройка
[ ] sudo cp /etc/fail2ban/jail.conf /etc/fail2ban/jail.local
[ ] sudo nano /etc/fail2ban/jail.local
    # [sshd]
    # enabled = true
    # maxretry = 3

# Запуск
[ ] sudo systemctl enable fail2ban
[ ] sudo systemctl start fail2ban

# Проверка
[ ] sudo fail2ban-client status sshd
```

---

## ✅ Финальная проверка

### 1. Функциональность бота

```bash
[ ] Бот отвечает на /start
[ ] Бот отвечает на /help
[ ] Можно выбрать язык
[ ] Можно создать заявку
[ ] Админ получает уведомление
[ ] /my_leads показывает заявки
[ ] Можно редактировать заявку
[ ] Можно удалить заявку
[ ] Можно загрузить файлы
[ ] AI улучшение описания работает
```

### 2. Системные проверки

```bash
[ ] Сервис запущен: sudo systemctl status telegram-lead-bot
[ ] Автозапуск включен: systemctl is-enabled telegram-lead-bot
[ ] Логи без критических ошибок: sudo journalctl -u telegram-lead-bot -n 100
[ ] База данных создана и доступна: ls -lh leads.db
[ ] Права доступа корректны: ls -la .env leads.db
```

### 3. Backup и мониторинг

```bash
[ ] Backup скрипт работает: ./scripts/backup-bot.sh
[ ] Backup'ы сохраняются в /home/botuser/backups/
[ ] Cron задачи добавлены: crontab -l
[ ] Health check работает: ./scripts/check-bot-health.sh
```

### 4. Безопасность

```bash
[ ] SSH вход по паролю отключен
[ ] Файрвол включен и настроен: sudo ufw status
[ ] .env файл защищен (chmod 600)
[ ] База данных защищена (chmod 600)
[ ] Автоматические обновления включены
```

---

## 🚀 Запуск в продакшн!

Если все пункты отмечены ✅, ваш бот готов к продакшн использованию!

### Следующие шаги:

1. **Мониторинг**
   - Проверяйте логи первые несколько дней
   - Убедитесь, что backup'ы создаются автоматически
   - Проверяйте health check регулярно

2. **Документация**
   - Сохраните копию этого чеклиста с отметками
   - Запишите все важные credentials в безопасное место
   - Документируйте любые изменения конфигурации

3. **Обновления**
   - Следите за обновлениями в репозитории
   - Тестируйте обновления на staging окружении (если есть)
   - Создавайте backup перед каждым обновлением

4. **Поддержка**
   - Настройте оповещения об ошибках
   - Регулярно проверяйте работоспособность
   - Ведите лог инцидентов и их решений

---

## 📞 Контакты и поддержка

- **Документация**: [README.md](README.md)
- **Deployment Guide**: [DEPLOYMENT.md](DEPLOYMENT.md)
- **Quick Start**: [QUICKSTART.md](QUICKSTART.md)
- **Scripts**: [scripts/README.md](scripts/README.md)
- **GitHub Issues**: [создать issue](https://github.com/yourusername/telegram-lead-bot/issues)

---

## 📝 Примечания

Используйте это пространство для заметок о вашем конкретном развертывании:

```
Дата развертывания: _________________
IP адрес сервера: _________________
Имя пользователя: _________________
Особенности конфигурации:
_____________________________________
_____________________________________
_____________________________________

Возникшие проблемы и их решения:
_____________________________________
_____________________________________
_____________________________________
```

---

**Удачи с вашим продакшн развертыванием! 🎉**
